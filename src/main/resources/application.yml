server:
  port: 7000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
         git:
          default-label: master
          uri: https://github.com/joffrehermosilla/config_server_demo
          search-paths: config-data
      
